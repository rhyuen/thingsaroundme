"use strict";

const chai = require("chai");
const chaiHttp = require("chai-http");

chai.use(chaiHttp);

describe("External Data Sources are up.", () => {
    
    it("Eventbrite should be up.", (done) => {

    });
    
    it("Meetup should be up.", (done) => {

    });

    it("Cineplex should be up.", (done) => {

    });
});